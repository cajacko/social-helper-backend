<?php 

function get_ratings() {
    // Get all actions which have not been counted yet
    // Add and average to the existing score, can this be done dynamically? I think it can
    /*
    SELECT * FROM objectAccountActions
    INNER JOIN objectAccountActionMeta

    */
}
